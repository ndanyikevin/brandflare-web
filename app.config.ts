import { defineConfig } from "@solidjs/start/config";

export default defineConfig({
  // server: {
  //   preset: "node", // Tells Vinxi to optimize for Bun's runtime
  // },
  // vite: {
  //   ssr: {
  //     // Ensure these stay on the client to avoid template errors on server
  //     noExternal: ["@kobalte/core", "lucide-solid"],
  //   }
  // }
});